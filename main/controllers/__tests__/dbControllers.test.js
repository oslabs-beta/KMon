// const db = require("../../models/db");
// const dbControllers = require("../dbControllers");

// jest.mock('../../models/db');

// saveConnections --

// important part is to make sure the query is formed correctly

// check if db.query is called with the right query and values.

// check that res.locals.response has some response in it,


