// const express = require("express");
// const router = express.Router();
// const graphControllers = require("../controllers/graphControllers");

// router.post("/creategraph", graphControllers.createGraph, (req, res) => {
//   res.status(201).json({ graph_id: res.locals.graph });
// });

// router.get("/allgraph", graphControllers.getGraphs, (req, res) => {
//   res.status(201).json({ graphs: result.rows });
// });

//module.exports = router;
